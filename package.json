{
  "dependencies": {
    "bootstrap": "^5.3.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.77.6"
  },
  "scripts": {
    "watch": "sass --watch src/scss:dist/css & live-server",
    "start": "live-server",
    "dev": "npm-run-all --parallel watch start"
  }
}
